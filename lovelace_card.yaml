# =============================================================================
# SMART CLOCK - LOVELACE DASHBOARD CARDS
# =============================================================================
# This file has TWO cards:
#
#   CARD 1 (below, "type: vertical-stack") - the main card. Runs entirely on
#           the MQTT-discovered entities. Paste it as a Manual card.
#
#   CARD 2 (near the bottom, "type: grid") - optional "quick messages". Needs
#           the smart_clock.yaml package loaded (HA restart required).
#           Paste it as a SECOND Manual card only after the package is in.
#
# Entity ids assume HA slugified the device as smart_clock_<name>. If HA
# appended _2 to any (name collision), fix the ids here.
# =============================================================================


# ===== CARD 1 - main (no package needed) =====================================
type: vertical-stack
cards:

  - type: markdown
    content: >
      # {{ states('sensor.smart_clock_time') }}

      **{{ states('sensor.smart_clock_date') }}**  -  showing:
      {{ states('sensor.smart_clock_display') }}

  - type: entities
    title: Message
    show_header_toggle: false
    entities:
      - entity: text.smart_clock_message
        name: Scroll text
      - entity: number.smart_clock_message_repeats
        name: Repeat count

  - type: horizontal-stack
    cards:
      - type: button
        name: Clock
        icon: mdi:clock
        tap_action: { action: toggle }
        entity: button.smart_clock_show_clock
      - type: button
        name: Date
        icon: mdi:calendar
        tap_action: { action: toggle }
        entity: button.smart_clock_show_date
      - type: button
        name: Restart
        icon: mdi:restart
        tap_action: { action: toggle }
        entity: button.smart_clock_restart

  - type: entities
    title: Display
    show_header_toggle: false
    entities:
      - entity: number.smart_clock_brightness
        name: Brightness (day)
      - entity: number.smart_clock_night_brightness
        name: Brightness (night)
      - entity: switch.smart_clock_night_dimming
        name: Night dimming (22:00-07:00)
      - entity: switch.smart_clock_12_hour_format
        name: 12-hour face

  - type: entities
    title: Diagnostics
    show_header_toggle: false
    entities:
      - entity: sensor.smart_clock_wi_fi_signal
        name: Wi-Fi signal
      - entity: sensor.smart_clock_ip_address
        name: IP
      - entity: sensor.smart_clock_mac_address
        name: MAC
      - entity: sensor.smart_clock_uptime
        name: Uptime
      - entity: sensor.smart_clock_free_heap
        name: Free heap
      - entity: sensor.smart_clock_wi_fi_reconnects
        name: Wi-Fi reconnects


# ===== CARD 2 - optional quick messages (needs smart_clock.yaml) ============
# Paste separately as a second Manual card, AFTER the package is loaded and
# HA restarted. Until then these buttons show "action not found".
#
# type: grid
# columns: 2
# square: false
# cards:
#   - type: button
#     name: Weather
#     icon: mdi:weather-partly-cloudy
#     tap_action:
#       action: perform-action
#       perform_action: script.smart_clock_show_weather
#   - type: button
#     name: Air / CO2
#     icon: mdi:molecule-co2
#     tap_action:
#       action: perform-action
#       perform_action: script.smart_clock_show_air
#   - type: button
#     name: Light
#     icon: mdi:brightness-5
#     tap_action:
#       action: perform-action
#       perform_action: script.smart_clock_show_light
#   - type: button
#     name: Random Quote
#     icon: mdi:comment-quote
#     tap_action:
#       action: perform-action
#       perform_action: automation.trigger
#       target:
#         entity_id: automation.smart_clock_random_quote
#   - type: button
#     name: Preset
#     icon: mdi:message-fast
#     tap_action:
#       action: perform-action
#       perform_action: script.smart_clock_show_preset
